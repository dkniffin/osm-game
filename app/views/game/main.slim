.header
  .resources
    .food
      = image_tag('food.png')
      span.key Food:
      span.value
        = @character.food
    .water
      = image_tag('water.png')
      span.key Water:
      span.value
        = @character.water
    .dev-console
      span.key Health:
      = link_to image_tag("plus.png"), "#", id: "restore-health"
      = link_to image_tag("minus.png"), "#", id: "take-damage"
  .user-actions
    - if signed_in?
      = link_to 'Log out', destroy_user_session_path, method: :delete
    - else
      = link_to 'Log in', new_user_session_path
#map

= render 'game/sidebar'
